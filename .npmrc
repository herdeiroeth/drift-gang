# DriftGang — npm/pnpm config
# Ensure devDependencies are installed in CI/production too
# (Vite and other dev tools are required for building this browser game)
ignore-scripts=false
engine-strict=false
node-options=
